- code = partial("embed_widget", {title: title, url: url_with(url)})
.widget_wrapper!= code
.links
  h2 Add this to your website:
  p Copy and paste embed code
  input.large(type='text', value="<h1>#{title}</h1>"+ code)
  h2 Link to this:
  p Copy and paste
  input.large(type='text', value=url_with("/data-file"))