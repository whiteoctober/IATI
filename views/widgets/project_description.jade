.widget_content
  h3.title Project description
  .scroller
    p!= description.replace(/\r\n|\r|\n/g, "<br>")

script
  inlines.push(function(){
    $(".scroller").height($("body").height() - $(".title").outerHeight(true));
  });